name: fluttergraphqlexample
description: Flutter GraphQL Example

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  graphql: ^3.0.2
  gql: ^0.12.3
  flutter_bloc: ^5.0.1
  get_it: ^4.0.2
  injectable: ^0.4.0+1
  equatable: ^1.2.0

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.0
  injectable_generator: ^0.4.1

flutter:
  uses-material-design: true
